apiVersion: v1
kind: Service
metadata:
  name: tiny-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: tiny-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}